'use strict';
var chai = require('chai'),
  MapsService = require('../../../services/MapsService'),
  sinon = require('sinon');

chai.use(require('chai-as-promised'));

describe('MapsService', function () {
  describe('_buildQuery', function() {
    it('should return a valid SQL query', function () {
      var result, data;
      data = {

      }
      // result = MapsService._buildQuery(data);
      // result.should.equal()
    });
  });
});
